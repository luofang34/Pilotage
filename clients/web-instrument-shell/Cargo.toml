[package]
name = "pilotage-instruments-web-shell"
description = "Thin WASM adapter over the Pilotage instrument runtime (ADR-0032): marshalling and packing only — no protocol dependency by construction (ADR-0034)"
version = "0.1.0"
edition.workspace = true
publish = false

[lib]
crate-type = ["rlib"]

[lints]
workspace = true

[dependencies]
indicate-instrument-feeder = { workspace = true }
indicate-instrument-state = { workspace = true }
pilotage-instrument-runtime = { workspace = true }
serde = { workspace = true }
serde-wasm-bindgen = { workspace = true }
wasm-bindgen = { workspace = true }
