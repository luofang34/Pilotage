[package]
name = "pilotage-instrument-runtime"
description = "The portable Pilotage instrument runtime: state decode and resolve, feeder state, alert step, panel configuration, scene generation and validation, production generations, typed producer status (ADR-0032)"
version = "0.1.0"
edition.workspace = true
publish = false

[lib]
crate-type = ["rlib"]

[lints]
workspace = true

[dependencies]
indicate-alerts = { workspace = true }
indicate-instrument-feeder = { workspace = true }
indicate-instrument-glyphs = { workspace = true }
indicate-instrument-panels = { workspace = true }
indicate-instrument-registry = { workspace = true }
indicate-instrument-scene = { workspace = true }
indicate-instrument-state = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
